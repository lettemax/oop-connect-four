export default class Column {
    constructor() {

    }

    add(playerNum) {
        //stores players num in the bottom most entry in the column
    }
    getTokenAt(rowIndex){
        if(rowIndex == null){
            return null;
        }else{
            // returns one if player one's token is there and likewise for player two
        }
    }
    isFull(){
        if(){// if column is full then return true
    }
}